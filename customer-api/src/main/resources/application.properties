spring.application.name=customer-api
spring.mongodb.uri=mongodb://localhost:27018/customerapi
spring.mongodb.database=customerapi
server.error.include-message=always

# Dev-only default secret (base64, 256-bit, for HS256). A real deployment
# should override this via the JWT_SECRET env var rather than committing a secret.
jwt.secret=${JWT_SECRET:76FPBJE8eA8detY+b60ZVIBtqB2N3sIaaxZ/dvmwpds=}
jwt.expiration-ms=28800000
